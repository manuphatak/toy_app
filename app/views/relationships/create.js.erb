$(".follow_form").html(
  "<%= escape_javascript(render('users/unfollow', user: @relationship.followed)) %>"
)

$(".stats > .stat.followers > strong").html(
  "<%= @relationship.followed.followers.count %>"
)
