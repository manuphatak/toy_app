<%= bootstrap_form_with(model: current_user.active_relationships.build(followed: user)) do |f| %>
  <%= f.hidden_field :follower_id %>
  <%= f.hidden_field :followed_id %>
  <%= f.primary 'Follow', extra_class: 'mt-2 w-100' %>
<% end %>
