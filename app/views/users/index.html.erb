<% provide(:body_class, 'bg-light') %>

<div class="container">
  <h1>Users</h1>

  <%= will_paginate @users %>
  <ul id="users" class="list-unstyled"><%= render @users %></ul>
  <%= will_paginate @users %>
</div>
